# 본선 산출물 제출 체크리스트 — 7/20(월) 10:00 마감

> 데이콘 안내(본선 진출 후보팀) 기준 · 팀장 구글드라이브 제출.
> 표의 [사람] 표시는 실PC/계정에서만 가능한 작업(빌드·녹화·업로드)입니다. 나머지는 저장소에 준비 완료.

## 팀 정보 (확정)

- **대학**: 영남대학교 · **팀명**: 팀 모두봄 · **팀원**: 김주형·류다영·신주현·이준영·장지웅
- **파일명의 '팀명'은 데이콘 등록 팀명과 정확히 일치해야 합니다.** 아래 파일은 `모두봄_...`로 준비했으니,
 등록명이 다르면(예: `팀모두봄`) 업로드 전 그 이름으로 바꿔 주세요.

## 드라이브 폴더 구성 (데이콘 파일명 규칙 · 팀명=모두봄 가정)

> 제출처: 팀장 데이콘 이메일 계정으로 로그인 → 팀별 드라이브 폴더. 최종본만 남기고 이전 버전은 삭제.

```
├── 모두봄_GitHub링크.txt      ← docs/제출/드라이브-업로드/모두봄_GitHub링크.txt
│                                (또는 모두봄_전체코드.zip: 저장소 Code→Download ZIP · Docker 미사용)
├── 모두봄_실행가이드.pdf       ← docs/제출/심사위원-실행-가이드.pdf (이름만 변경)
├── 모두봄_데모링크.txt         ← docs/제출/드라이브-업로드/모두봄_데모링크.txt
├── 모두봄_데모파일.zip         ← [사람] v0.3.2 재빌드본(ModooBom-Agent.zip / Setup.exe) — §빌드
├── 모두봄_발표자료.pdf         ← docs/발표/본선-발표자료-초안.pdf (16장·약 10분 · 이름만 변경)
└── 모두봄_시연영상.mp4 (선택)  ← [사람] 녹화
```

## 체크리스트

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| 1 | [필수] 전체 코드 | 링크 준비 · public 확인 [사람] | GitHub main — Docker 미사용. `모두봄_GitHub링크.txt` |
| 2 | [필수] 데모 — 웹 링크 | 준비됨 | 라이브 배포(main 푸시 자동). `모두봄_데모링크.txt` |
| 3 | [필수] 데모 — 데스크탑 exe/zip | [사람] 빌드 | v0.3.2 재빌드(§빌드) — Releases의 app-v0.3.0은 구버전 |
| 4 | [필수] 데모 — 실행 가이드 | 준비됨(PDF 재렌더) | `심사위원-실행-가이드.pdf` · 수치 1,029·30종 동기화 |
| 5 | [필수] 발표자료 PDF (10분) | 16장 완료 | 평가항목(창의30·구현25·편의20·주제15·사용10) 대응 + 경산 현장검증 · 이모지 제거·전문 톤 · 팀 최종 검수 권장 |
| 6 | [선택] 시연 영상 | [사람] 녹화 | 대본: `docs/기획서자료/발표-대본.md` §시연 |

## §빌드 — 데스크탑 exe/zip v0.3.2 (Windows PC에서, 약 10분)

```
1) git pull                      ← main 최신
2) setup-local.bat               ← 최초 1회(venv-local + 프론트 빌드 준비)
3) build-installer.bat           ← dist-app → PyInstaller → ZIP → (Inno Setup 있으면) Setup.exe
4) backend\dist\ 의 모두봄-에이전트.zip(무설치) / 모두봄-설치.exe 를 드라이브에 모두봄_데모파일.zip 으로 업로드
5) 업로드 전 1회 실행 확인: 실행 → [발급 전 점검] 6항목 → 서류 1종 실발급 완주
```

> 신뢰 근거: 이 컨테이너에서 **같은 spec으로 PyInstaller 번들을 실제 빌드·기동**해
> v0.3.2 신기능(실측 프로브·프리플라이트 6항목 등) 모듈 포함을 전부 확인했습니다(2026-07-19).
> Windows 빌드에서 남는 변수는 Inno Setup 유무뿐(없으면 ZIP만 제출해도 무설치 실행 가능 — 가이드에 안내됨).

## 발표자료 구성 (2026-07-19)

3분 라이브 피치를 전문가 검증용 10분 덱(16장)으로 확장하고, 디자인을 정리한 뒤 이모지와 과장 표현을 제거했습니다.
평가 항목별로 다음 슬라이드가 대응합니다.

- 한눈에 보는 모두봄 — 6개 핵심 지표 요약
- 차별성 비교표 — 복지로·보조금24·웰로 대비 (창의성)
- 온디바이스 다국어 AI 의미검색 — 핵심 기술 (창의성)
- 3층 아키텍처 — 온디바이스·LangGraph·RPA (구현 완성도)
- 데이터 구성 — 출처별 실데이터 도넛 (구현 완성도)
- 사용자 편의성 — 큰글씨·음성·TTS·통화·다국어·PWA (편의성)
- 현장 검증 — 경산시 노인복지센터·노인종합복지관 시연 (사용 가능성)
- 평가 항목 대응 요약 — 5개 항목별 근거

## 제출 직전 최종 점검(5분)

- [ ] 파일명이 **데이콘 등록 팀명**과 일치하는가(`팀명_전체코드`/`팀명_실행가이드`/`팀명_데모링크`/`팀명_발표자료`…)
- [ ] 드라이브 폴더가 위 구성과 일치하고 **팀장 계정**으로 업로드됐는가
- [ ] exe/zip을 드라이브에서 다시 내려받아 **새 PC 관점으로 1회 실행**되는가(PC 보호 창 → 실행)
- [ ] 발표자료 PDF가 드라이브 미리보기에서 폰트 깨짐 없이 보이는가
- [ ] GitHub 저장소가 **public**이고 README 첫 화면에 실행법이 보이는가
- [ ] 발표자료·실행가이드의 테스트 수치(1,029·30종)가 **제출 시점 실제와 일치**하는가 — 코드 세션 변경분 반영
- [ ] (영상 제출 시) 재생 확인 + 개인정보(실명·주민번호) 노출 프레임 없는가
