# 📋 사용자(사람)가 해야 할 일 — 한눈에

> Claude가 자율 작업 중 "사람만 할 수 있는 일"을 모아둔 목록. (최종 갱신: 7/6)
> ⏰가 붙은 것은 마감이 있는 것.
>
> **🚨 지금 가장 급한 것: 내일(7/7 10:00) 기획서 PDF 제출.** 아래 ⏰ 표 첫 줄부터.

## ⏰ 대회 제출 (마감순)
| 기한 | 할 일 | 준비물(이미 완성) |
|---|---|---|
| **7/7(화) 10:00** | DACON에 **기획서 PDF** 제출 — 코드공유 탭 비공개 게시. **일찍 낼수록 동점자 유리** | `docs/제출/모두봄-예선산출물-작성본.pptx`에 **팀명·대학·팀원 3칸만 입력** → 슬라이드 1~5를 PDF 내보내기 |
| **7/15(수) 10:00** | **최종 산출물 PDF**(11장 전체) + (선택·강력권장) 시연영상 링크 | 슬라이드 6~11 초안 완성돼 있음(`양식-슬라이드-원고.md`) — 스크린샷·QR만 삽입. 영상은 30초~1분(휴대폰 녹화면 충분) |
| **~7/17(금) 10:00** | **참가자 투표** — 팀장 계정으로 5표 **전부** 사용(본인 팀 불가, 미사용표 적을수록 동점자 유리) | 30초 어필 동선: `docs/기획서자료/발표-대본.md` §B |
| **7/20(월) 10:00** | 본선 진출(후보)팀 **전체 코드** 제출 — github 링크로 대체 가능 | 레포 public 유지만 하면 됨 |
| 7/31(목) 10:00 | 본선 발표자료(**PDF만**)·포스터 1장 제출 · 본선 8/11(화) 발표(한국어·2인) | 대본·심사기준 매핑 완비 |

## 🔑 계정·키가 필요한 일 (Claude가 대신 못 함)
- **data.go.kr API 키 발급** → `backend/.env`에 `DATA_GO_KR_SERVICE_KEY=` 입력
  - 이후 개별 데이터셋 "활용신청"(즉시 승인): 중앙부처복지서비스(15090532) 등
  - 키만 있으면 Claude가 ETL 확장으로 수천 건 추가 수집 가능
- **Render(무료) 백엔드 배포** — 심사장에서 챗 "🧠 실시간 AI(Claude)" 라벨을 보여주려면 필요
  - 순서: render.com 가입 → New Web Service → repo 연결 → `backend/` · `uvicorn main:app` → 환경변수 `ANTHROPIC_API_KEY` 입력
  - 배포 URL을 `frontend/.env.production`의 `VITE_API_BASE=`에 넣고 `npm run deploy`
- (선택) **Supabase 프로젝트 생성** — 로그인·동기화 기능 켜려면 `supabase/SETUP.md`
- (선택·격하됨) **크롬 웹스토어 등록($5)** — 이제 서류발급 기본 경로가 **무설치 전자증명서**로 바뀌어(7/6)
  확장은 '고급 자동화 시연'용. 데모는 확장 없이도 완주되므로 웹스토어 등록은 급하지 않음.

## 🏛️ 정부 공식 연계 (프로덕션 로드맵 — 기획서 '향후 계획'에 넣으면 강력)
> 지금 당장 할 필요는 없지만, **기획서에서 "우리는 스크래핑 꼼수가 아니라 정부 공식 유통망과 연계한다"**로
> 제시하면 심사에서 성숙도가 크게 올라감. 실제 이용신청은 대회 이후.
- **전자증명서 API 연계**(`dpaper.kr`) — 발급 서류를 전자문서로 제3자(모두봄) 제출. API 이용신청서를 운영부서에
  공문·우편 접수 → API KEY·지갑주소 발급. (개인 제출형이라 우리 '개인정보 온디바이스' 원칙과 잘 맞음)
- **공공 마이데이터 이용기관 등록**(`adm.mydata.go.kr`) — 본인 동의로 주민등록·과세증명 등 249종을 API 수신.
  이용기관 신청서·이용환경 조사표 등 서류 접수 필요(기관/사업자 등록 전제라 대회용은 로드맵으로).

## 🧪 실계정·실기기 리허설 (본선 전 1회)
- 정부24 **실계정으로 서류 자동발급 완주**(카카오 인증 포함) — 데스크탑 헤디드, `extension/validate_live.py` 참고
- 시연 노트북에서 **프리워밍**: AI 의미검색 토글 1회(모델 캐시) + PWA 오프라인 1회 로드
- **백업 영상 2개** 휴대폰/노트북에 저장: 서류발급 완주 15초 · 챗 시연 10초
- 폰 카카오톡 로그인 상태 확인

## 💡 참고(안 해도 되는 것)
- 기획서/산출물 문구·수치는 저장소 문서와 일치(과장 없음) — 제출 직전 `npm test` 숫자만 최신화하면 됨
- 피드백 메일 주소는 푸터 '의견 보내기'(6wngud@gmail.com) — 다른 주소를 원하면 `Footer.tsx` 한 줄 수정
