fastapi==0.115.5
uvicorn[standard]==0.32.1
websockets==13.1
python-dotenv==1.0.1

# LangGraph / LangChain
langgraph==0.2.56
langchain==0.3.12
# ⚠️ provider 패키지는 langchain-core 0.3.x 대역에 고정한다. 상한을 안 두면(>=2.0.0 등)
#    pip이 최신(genai 2.1+/core 1.x)을 끌어와 langgraph 0.2.56·langchain 0.3.12와 충돌 →
#    배포 이미지에서 import 실패 → 챗이 규칙 폴백으로 떨어진다. (dry-run으로 확인: genai 2.0.11 + core 0.3.63)
langchain-anthropic>=0.3.0,<0.4.0
langchain-community==0.3.12
# 무료 티어 AI provider (선택 — 키가 있으면 agents/llm.py가 자동 선택: Gemini > Groq > Anthropic)
langchain-google-genai>=2.0.0,<2.1.0
langchain-groq>=0.2.0,<0.3.0

# ChromaDB
chromadb==0.5.23

# Data / Utils
pydantic==2.10.3
pydantic-settings==2.6.1
httpx==0.27.2
python-multipart==0.0.20

# RPA / Browser Automation
playwright>=1.44.0
