# 발표 시연 가이드 (DEMO_GUIDE)

> 배포 웹(github.io)은 누구나 AI 복지분석·추천·챗봇·검색을 쓸 수 있다.
> **실제 서류 자동발급/자동신청 RPA**는 두 경로가 있다:
> **(A) 크롬 확장 — 권장**(배포 사이트에서 바로, 파이썬 불필요) / (B) 로컬 에이전트(백엔드).

---

## 🟢 권장 시연 경로 A — 크롬 확장 (배포 사이트에서 바로)

파이썬·로컬서버 없이 **배포된 라이브 사이트**에서 실제 정부24 서류가 자동 발급된다.

### 사전 준비 (발표 30분 전)
1. 크롬 `chrome://extensions` → **개발자 모드 ON** → **압축해제된 확장 프로그램 로드** → 저장소 `extension/` 선택. (작업 PC는 루트 `update.bat` 더블클릭 후 새로고침 ↻로 최신화)
2. 카카오톡 설치된 스마트폰(간편인증용).
3. 라이브 사이트 → **나의 복지 → 서류 준비 도우미 → 자동입력 추가정보**에 **실명·생년월일·휴대폰** 입력(내 기기에만 저장).
4. **개발자도구(F12)는 닫아둔다** — 열려 있으면 '진짜 클릭'이 실패한다.
5. (선택) AI 검색 토글 1회 켜서 모델 프리워밍.

### 시연 동선
1. 서류 준비 도우미 **[등본 자동발급]**(또는 🚀 여러 서류 전부) → 새 탭에서 정부24 로그인 자동 진행.
2. **간편인증 창 자동 오픈** + 카카오톡 선택 + 이름·생년월일·휴대폰·전체동의 자동 입력.
3. 📱 '인증 요청' + 카카오톡 [인증 허용] (본인인증만 사람).
4. 발급 폼 자동 → 5초 검토 카운트다운 → **신청 자동 클릭** → PDF 자동 저장.
5. 멘트: "방금 서버를 하나도 안 거쳤습니다. 브라우저 확장이 직접 한 겁니다."

### 막히면
- 확장 팝업 또는 웹 화면 하단 **[🔍 진단 복사]** → 원인 특정. **[⛔ 작업 취소]**로 재시작.
- 상단 "…디버깅하고 있습니다" 배너는 정상(진짜 클릭 생성 중).

---

## 경로 B — 로컬 에이전트로 실제 RPA 시연 (백업 플랜)

> 개인정보 보호·본인인증(headed 브라우저) 때문에 **발표자 PC의 로컬 에이전트**에서 동작. 아래는 그 절차.

## 0. 사전 준비 (발표 30분 전)
- Windows PC + Python 3.11 + 크롬(Playwright chromium)
- 카카오톡 설치된 스마트폰(간편인증용)
- 저장소 클론 + 백엔드 의존성 설치:
  ```bash
  cd backend
  python -m venv venv && venv\Scripts\activate
  pip install -r requirements.txt
  python -m playwright install chromium
  ```
- (선택) `.env` 에 `ANTHROPIC_API_KEY` 넣으면 로컬에서도 진짜 AI(Claude) 분석.
  안 넣으면 Mock(규칙기반)으로 동작(시연엔 충분).

## 1. 로컬 에이전트(백엔드) 실행

### 🟢 가장 쉬움 — 원클릭 (배포 사이트와 함께 쓸 때)
- **`run-agent.bat` 더블클릭.** 백엔드(로컬 에이전트)만 뜨고, 최초 실행 시 venv 생성·의존성·브라우저 설치까지 자동.
  이후 자동으로 배포 사이트(https://biocode67.github.io/modoo-bom/)가 열리고 에이전트를 감지해 RPA가 켜진다.

### 수동 (개발자용)
```bash
cd backend && venv\Scripts\activate
uvicorn main:app --port 8000
```
- 확인: http://localhost:8000/api/health → `capabilities.rpa: true` 여야 함.
  (로컬이므로 자동으로 rpa=true. headed 브라우저로 뜬다.)

## 2. 웹 접속 & 자동 감지
- 브라우저(**크롬 권장**)로 배포 웹 https://biocode67.github.io/modoo-bom/ 접속
  또는 로컬 프론트 `cd frontend && npm run dev` → http://localhost:5173
- 프론트가 `localhost:8000` 로컬 에이전트를 감지 → **"자동발급 가능" 배지**가 뜨고
  RPA 버튼이 활성화된다.
  - 크롬은 http://localhost 를 신뢰 출처로 보고 PNA 헤더(백엔드가 응답)로 통과한다.
  - ⚠️ Firefox/Safari는 로컬 http 호출을 막을 수 있으니 **크롬으로 시연**.

## 2.5 대안 경로 — 크롬 확장(로컬 서버조차 불필요, 권장 시연)
> 백엔드 없이도 **사용자 브라우저 안에서** 서류발급·복지신청을 자동화하는 확장이 핵심 기능.
1. `chrome://extensions` → 우측 상단 **개발자 모드** ON → **압축해제된 확장 프로그램 로드** → 저장소의 `extension/` 폴더 선택
2. 배포 웹 접속 → 확장을 자동 감지해 **자동발급/자동신청 버튼 활성화** (로컬 백엔드 불필요)
3. 이후 흐름은 시나리오 A/B와 동일(간편인증·최종제출만 발표자 직접). 지원: 정부24·복지로·건보·고용24·국민연금·장학재단 6사이트
- 웹스토어 제출 패키지는 `extension/build.ps1`·`STORE.md` 참고. 확장 동작이 막히면 아래 로컬 에이전트 경로로 폴백.

## 3. 시나리오 A — 서류 자동발급 (정부24 주민등록등본)
1. '나의 복지' 또는 정책 상세 → **서류 준비 도우미**에서 등본 옆 **[자동]** 클릭
2. 로컬 에이전트가 정부24를 headed 크롬으로 열고 **간편인증 화면까지 자동 진행**
3. 🔐 **여기서 발표자가 직접**: '간편인증 → 카카오톡' 선택 → 이름/생년월일/휴대폰 입력 →
   '인증 요청' → **스마트폰 카카오톡 알림에서 [인증 허용]**
4. 인증 후 RPA가 이어서 발급 페이지로 진행, 문서를 로컬(사용자 PC)에 저장
   - 개인정보는 **서버로 전송/저장하지 않음**(로컬에서만 처리, 화면 스크린샷도 사용자 세션 내에서만)

## 4. 시나리오 B — 복지 자동신청 (복지로)
1. 정책 상세(예: 기초연금/아동수당) → **에이전트 자동 신청** → [에이전트로 신청 시작]
2. 로컬 에이전트가 복지로 로그인→간편인증 위젯까지 자동 도달
3. 🔐 발표자가 카카오 본인인증 직접 수행
4. 인증 후 신청 양식으로 이동 + 이름/생년월일/연락처 **자동 입력**
5. ⚠️ **최종 제출은 발표자가 화면 확인 후 직접** 클릭(비가역·법적 행위라 의도된 안전장치)

## 5. 실패 시 대처 (정부 사이트 DOM은 자주 바뀜)
- **자동입력이 안 들어가면**: 화면 안내대로 직접 입력 후 계속 진행(수동 폴백 문구 표시됨).
- **간편인증 위젯이 안 뜨면**: 브라우저에서 '간편인증' 버튼을 직접 눌러 위젯을 띄운 뒤 진행.
- **RPA 버튼이 아예 안 보이면**: (1) 로컬 에이전트가 켜졌는지 `/api/health` 확인,
  (2) 크롬으로 접속했는지, (3) `RPA_ENABLED=1` 로 강제 활성 후 재시작.
- **최후 폴백**: 웹의 공식 신청 링크(복지로/정부24)로 수동 신청 시연. 데이터·분석은 그대로 시연 가능.

## 6. 시연 추천 흐름 (5분)
1. (웹) 프로필 입력 → **"놓치고 계실 현금성 지원 N개, 월 예상 OOO원"** 헤드라인 강조
   - 차별점 멘트: "중앙부처+지자체 약 5,000건에 **민간재단(현대차 정몽구 스칼러십·심장재단 수술비 등)까지** —
     정부 사이트 어디에도 한곳에 없는 조합" (관련 복지 섹션에 💝 배지로 노출)
2. (웹) **챗 에이전트 시연**: 말풍선 열기 → 급한 마감·서류가 있으면 먼저 브리핑됨 →
   "내가 받을 수 있는 거 알려줘" → 자격+이유 답변 → **"다 담아줘"** 한마디로 관심목록 저장까지
   (검색봇이 아니라 '나를 알고 대신 행동하는' 에이전트임을 강조. 다국어 의미검색도 이어서 가능)
3. (확장 또는 로컬) 서류 자동발급 → 카카오 인증 → 발급까지 실제 시연
4. 마무리: "AI는 클라우드, 개인정보 다루는 RPA는 내 브라우저/PC에서 — 개인정보는 서버에 안 남는다" 메시지

## 7. 개인정보 안전 포인트(심사 어필)
- 인증정보/주민번호 등은 **서버 저장·로깅 없음**. 로컬 메모리에서만 처리 후 폐기.
- 클라우드 백엔드는 RPA를 아예 실행하지 않음(`rpa:false`, 엔드포인트 503).
- 발급 서류는 발표자 PC에만 저장.
